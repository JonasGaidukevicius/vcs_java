package lt.gaidukevicius;

public class SurioPica extends Pica {
	
	SurioPica(int padas, int suris, int grybai, int kumpis, int desra, int padazas){
		this.produktai.setPadas(padas);
		this.produktai.setGrybai(grybai);
		this.produktai.setKumpis(kumpis);
		this.produktai.setDesra(desra);
		this.produktai.setPadazas(padazas);
	}
}
